import React, { useState } from "react";
import { recentLocationContainerProps } from "./types/recentLocationTypes";

const RecentLocationsContainer: React.FC<recentLocationContainerProps> = (

) => {
  const [location, setLocation] = useState<>()

  return (
    
  )
};

export default RecentLocationsContainer;